// test("hello world", () => {});

// test("this should fail", () => {
//   throw new Error("failure");
// });

// test("Async test demo (done)", (done) => {
//   setTimeout(() => {
//     expect(1).toBe(2);
//     done();
//   }, 2000);
// });

// test("Async test demo (async)", async () => {
//   const result = await setTimeout(() => expect(1).toBe(2), 2000);
//   return result;
// });

/* enter--->(npm test) in terminal to test */
